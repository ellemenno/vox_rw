name: vox_rw
description: read and write binary files in the MagicaVoxel `.vox` file format, with support for core and extension chunks
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # none

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
